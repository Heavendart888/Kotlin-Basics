fun main() {
    var userName:String = "Biswa"
    userName="Ram"

    var age:Int=22
    age=25

    val city="rou"
    //if also you do not give the type of variable it automatically infere the type that you give to that variable above one is an example.

    //val keyword do not allow to change the value in futher were as var keyword does allow to change the value.

    //variable name must be in camelCase.
    println("Hello $userName your age is $age")
}